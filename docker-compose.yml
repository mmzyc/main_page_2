version: '3.8'

services:
  mafl:
    image: ghcr.io/hywax/mafl
    restart: unless-stopped
    ports:
      - '3888:3000'
    volumes:
      - ./data:/app/data
      - ./data/favicons/favicons.png:/app/public/icons/favicons.png
